#include<stdio.h>
int main()
{
    int i,a,b,
    printf("enter two number");
    scanf("%d%d",&a,&b);
    for(x=a;x<=b;x++)
    {
    for(i=2;i<=;i++)
        if(x%i==0)
        break;
        if(i==x)
        {
            printf("%d  ",x);
        }
    }
        
}