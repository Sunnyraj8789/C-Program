#include<stdio.h>
int main()
{
    int x,y,z,a;
    x=sizeof(int);
    y=sizeof(double);
    z=sizeof(char);
    a=sizeof(float);
    printf("size of x is %d\n",x);
    printf("size of y is %d\n",y);
    printf("size of z is %d\n",z);
    printf("size of a is %d",a);
     
     return 0;
}